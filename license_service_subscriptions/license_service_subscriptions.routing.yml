# Routes for the license_service_subscriptions sub-module.
#
# Standard CRUD routes for the LicenseSubscriptionPlan config entity are
# auto-generated by AdminHtmlRouteProvider (declared in the entity annotation).
# This file defines any additional routes beyond the entity CRUD set.
#
# Phase 3 will add:
#   - /admin/config/license-service/subscriptions/settings (deprecation config)
#   - /user/{user}/subscriptions/choose (tokenised choose-plan page)
#   - /admin/config/license-service/subscriptions/subscribers (report)

# Placeholder — no additional routes in Phase 1 scaffolding.
# Remove this comment block and add routes here in Phase 3.
