<?xml version="1.0"?>
<ruleset name="license_service_token_counter">
  <description>PHP_CodeSniffer rules for the License Service Token Counter module.</description>

  <!-- Scan these paths by default. -->
  <file>src</file>
  <file>tests</file>
  <file>license_service_token_counter.module</file>
  <file>license_service_token_counter.install</file>
  <file>modules/license_service_token_counter_engine/src</file>
  <file>modules/license_service_token_counter_engine/tests</file>

  <!-- Show sniff codes in output. -->
  <arg value="sp"/>
  <arg name="colors"/>
  <arg name="extensions" value="php,module,install,inc"/>

  <!-- Drupal coding standard (requires drupal/coder). -->
  <rule ref="Drupal"/>
  <rule ref="DrupalPractice"/>

  <!-- Allow long lines in tests (assertion chains can be verbose). -->
  <rule ref="Drupal.Files.LineLength">
    <exclude-pattern>tests/*</exclude-pattern>
    <exclude-pattern>modules/*/tests/*</exclude-pattern>
  </rule>

  <!-- Security: flag risky functions. -->
  <rule ref="Generic.PHP.ForbiddenFunctions">
    <properties>
      <property name="forbiddenFunctions" type="array">
        <element key="eval" value="null"/>
        <element key="exec" value="null"/>
        <element key="shell_exec" value="null"/>
        <element key="passthru" value="null"/>
        <element key="system" value="null"/>
        <element key="popen" value="null"/>
        <element key="proc_open" value="null"/>
        <element key="var_dump" value="null"/>
        <element key="print_r" value="null"/>
      </property>
    </properties>
  </rule>

</ruleset>
